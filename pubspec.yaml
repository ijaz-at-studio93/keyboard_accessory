name: keyboard_accessory
description: >-
  A floating glassmorphic keyboard accessory toolbar for Flutter with
  focus traversal, customisable actions, and full bar replacement.
version: 0.1.1
homepage: https://github.com/ijaz-at-studio93/keyboard_accessory
repository: https://github.com/ijaz-at-studio93/keyboard_accessory

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
